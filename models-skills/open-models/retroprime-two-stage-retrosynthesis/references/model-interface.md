# Model interface

RetroPrime runs a reaction-center stage followed by a retrosynthesis stage. Both checkpoints and their hashes are recorded in the manifest; a final candidate is valid only after both stages succeed.
