# GenMol skill

GenMol SAFE input and resource preflight.

Fixed request and preflight cases are archived in `examples/cases.json`. The current checked-in fixture is resource-blocked and must not be read as a successful molecule-generation output.
